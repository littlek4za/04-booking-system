import { EventResponseDto } from './event-response-dto';

describe('EventSaveResponseDto', () => {
  it('should create an instance', () => {
    expect(new EventResponseDto()).toBeTruthy();
  });
});
