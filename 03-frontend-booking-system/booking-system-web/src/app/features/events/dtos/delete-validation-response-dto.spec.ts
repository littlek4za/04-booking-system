import { DeleteValidationResponseDto } from './delete-validation-response-dto';

describe('DeleteValidationResponseDto', () => {
  it('should create an instance', () => {
    expect(new DeleteValidationResponseDto()).toBeTruthy();
  });
});
