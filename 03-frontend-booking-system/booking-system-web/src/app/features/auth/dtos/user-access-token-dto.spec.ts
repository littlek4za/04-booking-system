import { UserAccessTokenDto } from './user-access-token-dto';

describe('UserAccessTokenDto', () => {
  it('should create an instance', () => {
    expect(new UserAccessTokenDto()).toBeTruthy();
  });
});
