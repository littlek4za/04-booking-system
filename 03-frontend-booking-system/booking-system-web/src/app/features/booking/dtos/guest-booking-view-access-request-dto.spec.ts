import { GuestBookingViewAccessRequestDto } from './guest-booking-view-access-request-dto';

describe('GuestBookingViewAccessRequestDto', () => {
  it('should create an instance', () => {
    expect(new GuestBookingViewAccessRequestDto()).toBeTruthy();
  });
});
