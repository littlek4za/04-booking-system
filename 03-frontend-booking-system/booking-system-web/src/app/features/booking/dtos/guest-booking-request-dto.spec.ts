import { GuestBookingRequestDto } from './guest-booking-request-dto';

describe('GuestBookingRequestDto', () => {
  it('should create an instance', () => {
    expect(new GuestBookingRequestDto()).toBeTruthy();
  });
});
