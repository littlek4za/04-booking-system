import { GuestBookingViewInitRequestDto } from './guest-booking-view-init-request-dto';

describe('GuestBookingViewInitRequestDto', () => {
  it('should create an instance', () => {
    expect(new GuestBookingViewInitRequestDto()).toBeTruthy();
  });
});
