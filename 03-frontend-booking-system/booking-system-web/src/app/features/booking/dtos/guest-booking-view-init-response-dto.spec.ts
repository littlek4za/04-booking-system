import { GuestBookingViewInitResponseDto } from './guest-booking-view-init-response-dto';

describe('GuestBookingViewInitResponseDto', () => {
  it('should create an instance', () => {
    expect(new GuestBookingViewInitResponseDto()).toBeTruthy();
  });
});
