import { SlotBookedTimeResponseDto } from './slot-booked-time-response-dto';

describe('SlotBookedTimeResponseDto', () => {
  it('should create an instance', () => {
    expect(new SlotBookedTimeResponseDto()).toBeTruthy();
  });
});
