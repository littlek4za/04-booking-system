import { BookingResponseDto } from './booking-response-dto';

describe('BookingResponseDto', () => {
  it('should create an instance', () => {
    expect(new BookingResponseDto()).toBeTruthy();
  });
});
