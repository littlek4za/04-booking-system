import { InvitationResponseDto } from './invitation-response-dto';

describe('InvitationResponseDto', () => {
  it('should create an instance', () => {
    expect(new InvitationResponseDto()).toBeTruthy();
  });
});
