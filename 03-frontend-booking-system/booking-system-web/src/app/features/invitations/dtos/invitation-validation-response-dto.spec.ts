import { InvitationValidationResponseDto } from './invitation-validation-response-dto';

describe('InvitationValidationResponseDto', () => {
  it('should create an instance', () => {
    expect(new InvitationValidationResponseDto()).toBeTruthy();
  });
});
