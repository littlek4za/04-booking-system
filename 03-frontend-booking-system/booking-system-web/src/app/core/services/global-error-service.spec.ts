import { GlobalErrorService } from './global-error-service';

describe('GlobalErrorService', () => {
  it('should create an instance', () => {
    expect(new GlobalErrorService()).toBeTruthy();
  });
});
