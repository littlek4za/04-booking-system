import { AuthFormValidator } from './auth-form-validator';

describe('AuthFormValidator', () => {
  it('should create an instance', () => {
    expect(new AuthFormValidator()).toBeTruthy();
  });
});
